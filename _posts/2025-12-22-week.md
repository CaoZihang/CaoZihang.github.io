---
layout: post
title:  "【阅】本周阅读摘选2025-12-15 → 2025-12-21"
author: "Cao Zihang"
header-style: text
catalog: true
status: "working"
mathjax: true
tags:
  - 日常
  
  
---
<center style="margin-bottom: 20px; margin-top: 50px"><font color="#3879B1" style="line-height: 1.4;font-weight: 700;font-size: 36px;box-sizing: border-box; ">本周阅读摘选</font></center>


<center style=" margin-bottom: 30px;">2025-12-15 → 2025-12-21</center>

<font style="font-weight: bold;">目录</font>

* 目录
{:toc}


# 学术相关
## Is the Money Spent on Short-Form Video Social Platforms Worth It? The Role of Advertising Spillover in a Large-Scale Randomized Field Experiment on ByteDance [^1]

# 技术技巧

## [Python丨云朵君 - Python数据库选型](https://mp.weixin.qq.com/s/wfKghbWQIv4cnGhLHN0nFQ)

{:.academia}
| 名称  | 类型 | 特征 | 适用场景 |
| :---- | :---- | :---- | -------- |
| MySQL |关系型数据库      |成熟完善      |    结构化数据      |
|PostgreSQL| 关系型数据库| 强大的复杂数据规则；开源 | 复杂业务、GIS、JSON数据混合|
|SQLite| 嵌入式关系型数据库|轻量级；My Favorite | 应用、原型开发|
|MongoDB| 文档型数据库| 灵活数据类型|内容推送、日志存储、用户画像|
|Redis| 内存键值数据库| 极速读写小数据 | 缓存、会话、消息队列|
|Neo4j| 图数据库| 记录节点与关系|社交网络、推荐系统、知识图谱|
|Milvus|向量数据库| 云原生架构|LLM|

## UV

- 帮助 `uv [] --help`
- 更新 `pip install --upgrade uv`
- Python管理
  - 安装版本 `uv python install`
  - 列出版本 `uv python list`
  - 删除版本 `uv python uninstall`
  - 寻找版本 `uv python find`
  - 指定版本 `uv python pin`
- 脚本管理
  - 运行 `uv run`
  - 添加脚本级依赖 `uv add --script`
  - 删除脚本级依赖 `uv remove --script`
- 项目管理
  - 初始化 `uv init`
  - 添加项目依赖 `uv add`
  - 删除项目依赖 `uv remove`
  - 项目依赖与环境同步 `uv sync` (类似pip install -r)
  - 创建lockfile `uv lock`
  - 运行命令 `uv run`
  - 依赖树（显示声明的） `uv tree`
  - 打包 `uv build`
  - 发布 `uv publish` 需要配置仓库
- Tools
  - 临时应用包 `uvx / uv tool run`
    - 创建一个隔离环境，下载运行工具后清理，避免污染全局环境
    - 常用于pytest等工具
  - 安装到用户全局 `uv tool install`
  - 卸载用户全局包 `uv tool uninstall`
  - 列出用户全局包 `uv tool list`
- 环境管理
  - 创建虚拟环境 (通常uv init会自动创建，无需手动) `uv venv`
  - 安装到当前环境 `uv pip install`
  - 卸载环境包 `uv pip uninstall`
  - 显示安装包详情 `uv pip show`
  - 列出依赖 `uv pip freeze`
  - 易读列出 `up pip list`
  - 兼容性校验 `uv pip check`
  - 环境依赖树 `uv pip tree`
  - lockfile `uv pip compile`
  - 同步 `uv pip sync`

### 项目工作流

```cmd
# 初始化项目 生成配置文件
uv init
uv run main.py

# 添加依赖
uv add numpy
uv add 'pandas>=1.0.0"
uv add git+https://github.com/psf/request'
uv add -r requirements.txt -c constraints.txt
uv remove numpy
uv lock --upgrade-packages requests

# 发布
uv build
uv version 1.0.0
## 版本增量
uv version --bump [major|minor|patch|stable|aplpha|beta|rc]
uv publish

```

[^1]: Liang, Y. (Sky), Chen, X. (Jack), Han, S., Zhang, J., & Chen, Y. (2025). Is the Money Spent on Short-Form Video Social Platforms Worth It? The Role of Advertising Spillover in a Large-Scale Randomized Field Experiment on ByteDance. Marketing Science, 44(5), 1125–1144. https://doi.org/10.1287/mksc.2023.0575
